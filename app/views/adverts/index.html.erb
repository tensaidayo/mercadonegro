<p id="notice"><%= notice %></p>

<h1>Produtos</h1>

<table class="table">
  <thead>
    <tr>
      <th>Titulo</th>
      <th>Descricao</th>
      <th>Preço</th>
      <th>Categoria</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @adverts.each do |advert| %>
      <tr>
        <td><%= advert.title %></td>
        <td><%= advert.description %></td>
        <td><%= advert.price %></td>
        <td><%= advert.category.description %></td>
        <td><%= link_to 'Show', advert, class: "btn btn-primary active" %></td>
        <td><%= link_to 'Edit', edit_advert_path(advert), class: "btn btn-primary active" %></td>
        <td><%= link_to 'Destroy', advert, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-primary active" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Cadastrar Produto', new_advert_path, class: "btn btn-primary active" %>
