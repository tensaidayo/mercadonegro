<p id="notice"><%= notice %></p>

<h1>Produtos</h1>

<table class="table">
  <thead>
    <tr>
      <th>Titulo</th>
      <th>Descricao</th>
      <th>Preço</th>
      <th>Categoria</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @adverts.each do |advert| %>
      <tr>
        <td><%= advert.title %></td>
        <td><%= advert.description %></td>
        <td><%= advert.price %></td>
        <td>
          <div class="btn-group">
            <%= link_to 'Exibir', advert, :class => "btn btn-primary active" %></button>
            <%= link_to 'Editar', edit_advert_path(advert), :class => "btn btn-primary active" %>
            <%= link_to 'Excluir', advert, :class => "btn btn-primary active", method: :delete, data: {confirm: 'Are you sure?'} %></button>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Cadastrar Produto', new_advert_path, class: "btn btn-primary active" %>
